@model PBL3.Models.Entities.NewsFeed

@{
    ViewData["Title"] = "Chi tiết";
}

@{
    var html = Markdig.Markdown.ToHtml(Model.Content);
}
<h1>Details</h1>

<div>
    <h4>NewsFeed</h4>
    <hr />
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Content)
        </dt>
        <div>
            @Html.Raw(html)
        </div>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Lưu chỉnh sửa</a> |
    <a asp-action="Index">Trở lại</a>
</div>
